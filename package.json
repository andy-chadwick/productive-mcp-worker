{
  "name": "productive-mcp-worker",
  "version": "1.0.0",
  "description": "Productive.io MCP server on Cloudflare Workers (stateless, free tier)",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "typescript": "^5.5.2",
    "wrangler": "^3.99.0"
  }
}
